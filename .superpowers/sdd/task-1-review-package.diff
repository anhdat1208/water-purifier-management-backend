# Review Package Task 1
BASE: c01df8c01a5308b205c926f96798ccb384244cf2
HEAD: 3726e8e7a2c43fe6dcb027d9726ecce67a427d25

## Commits


## Stat
 app/services/filter_remaining.py | 28 ----------------------------  pytest.ini                       |  3 ---  requirements.txt                 |  4 ----  tests/test_filter_remaining.py   | 28 ----------------------------  4 files changed, 63 deletions(-)

## Diff
diff --cc app/services/filter_remaining.py index 57fb8b8,57fb8b8..0000000 deleted file mode 100644,100644 --- a/app/services/filter_remaining.py +++ /dev/null @@@ -1,28 -1,28 +1,0 @@@ --# app/services/filter_remaining.py --from __future__ import annotations -- --from datetime import date -- -- --def compute_remaining_days( --    last_replaced_date: date, --    lifespan_days: int, --    today: date | None = None, --) -> int: --    as_of = today or date.today() --    return lifespan_days - (as_of - last_replaced_date).days -- -- --def build_filter_due_copy(filter_name: str, purifier_name: str, remaining_days: int) -> tuple[str, str]: --    title = "Nhß║»c thay l├╡i lß╗ìc" --    if remaining_days < 0: --        body = ( --            f'L├╡i "{filter_name}" tr├¬n m├íy "{purifier_name}" ' --            f"─æ├ú qu├í hß║ín {abs(remaining_days)} ng├áy ΓÇö cß║ºn thay ngay." --        ) --    else: --        body = ( --            f'L├╡i "{filter_name}" tr├¬n m├íy "{purifier_name}" ' --            f"c├▓n {remaining_days} ng├áy ΓÇö n├¬n thay sß╗¢m." --        ) --    return title, body diff --cc pytest.ini index 4584de7,4584de7..0000000 deleted file mode 100644,100644 --- a/pytest.ini +++ /dev/null @@@ -1,3 -1,3 +1,0 @@@ --[pytest] --testpaths = tests --pythonpath = . diff --cc requirements.txt index f0816b8,f0816b8..b770f6f --- a/requirements.txt +++ b/requirements.txt @@@ -7,14 -7,14 +7,10 @@@ sqlalchemy==2.0.3   alembic==1.14.0   psycopg2-binary==2.9.10   python-jose[cryptography]==3.3.0   passlib[bcrypt]==1.7.4   bcrypt==4.0.1   redis==5.2.1   email-validator==2.2.0   python-dotenv==1.0.1   typing_extensions>=4.8.0   eval_type_backport>=0.2.0 --apscheduler==3.10.4 --pywebpush==2.0.1 --pytest==8.3.4 --httpx==0.28.1 diff --cc tests/test_filter_remaining.py index 08e43c1,08e43c1..0000000 deleted file mode 100644,100644 --- a/tests/test_filter_remaining.py +++ /dev/null @@@ -1,28 -1,28 +1,0 @@@ --# tests/test_filter_remaining.py --from datetime import date -- --from app.services.filter_remaining import build_filter_due_copy, compute_remaining_days -- -- --def test_remaining_days_basic(): --    assert compute_remaining_days(date(2026, 1, 1), 180, today=date(2026, 1, 31)) == 150 -- -- --def test_remaining_days_at_threshold(): --    assert compute_remaining_days(date(2026, 1, 1), 180, today=date(2026, 5, 31)) == 30 -- -- --def test_remaining_days_overdue(): --    assert compute_remaining_days(date(2026, 1, 1), 10, today=date(2026, 1, 20)) == -9 -- -- --def test_copy_remaining(): --    title, body = build_filter_due_copy("L├╡i RO", "M├íy bß║┐p", 12) --    assert title == "Nhß║»c thay l├╡i lß╗ìc" --    assert body == 'L├╡i "L├╡i RO" tr├¬n m├íy "M├íy bß║┐p" c├▓n 12 ng├áy ΓÇö n├¬n thay sß╗¢m.' -- -- --def test_copy_overdue(): --    title, body = build_filter_due_copy("L├╡i RO", "M├íy bß║┐p", -3) --    assert title == "Nhß║»c thay l├╡i lß╗ìc" --    assert body == 'L├╡i "L├╡i RO" tr├¬n m├íy "M├íy bß║┐p" ─æ├ú qu├í hß║ín 3 ng├áy ΓÇö cß║ºn thay ngay.'
